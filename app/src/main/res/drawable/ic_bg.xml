<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="10dp"
        android:color="@color/black"/> <!-- 默认的边框颜色 -->
    <corners android:radius="15dp"/>
</shape>