<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="15dp"
        android:color="@android:color/holo_blue_light"/> <!-- 选中的边框颜色 -->
    <corners android:radius="15dp"/>
</shape>